# Tuned Reconstruction Defaults

The committed JSON registry is the inference source of truth. This page
contains the complete selected-value table, model-specific exceptions,
inheritance behaviour, and command for auditing the validation-only search.

## Defaults and selection procedure

```{include} ../../../scripts/paper_scripts/PaDIS-Reproduction/README.md
:start-after: "## Tuned reconstruction defaults"
:end-before: "## Manual reconstruction and matrix inspection"
```
